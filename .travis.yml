sudo: required

language: python

python:
  - "3.6"

# install dependencies
before_install:
  - sudo apt-get install -y libasound2-dev libpulse-dev swig

# install project
install:
  - pip install .

# command to run tests
script:
  - pytest
